# 更新记录

## v0.2.0 — 2026-09-16

### 修复

- 时间轴点击改为定位到点击位置，点击和拖动时暂停播放，防止播放更新与用户操作争抢滑块。
- 解码忙碌时保留最后一次定位请求，丢弃过期结果；切换视频时不显示旧视频画面。
- 逐帧播放不再反复禁用、启用右侧功能栏和视频列表；标记列表仅在相关数据变化时刷新，减少闪烁并保持选择。
- 同尺寸帧更新不再重复调整画面缩放。

### 新增

- 视频剪辑画面预览、播放与逐帧定位。
- 当前帧设置起点、当前帧设置终点（含此帧）、选择全部。
- 绿色时间轴选区、选区预览，以及与秒数输入同步。
- 窗口和应用版本号。

### 验证

- 完整 unittest 测试集共 15 项通过，新增 7 项交互及回归测试。
- 固定帧率样例选择第 24～47 帧，实际导出为 1 秒、24 帧。
- 已检查 Qt 剪辑页面截图；截图保存在本地 `test-output/`，不纳入版本控制。

### 当前限制

- 剪辑仍为单区间截取，不含拼接、多区间删除或可拖动的选区边界手柄。
- 预览不播放音频；输出保留可选首条音轨。
- 时间按帧号和 FPS 换算，可变帧率精确时间戳尚未实现；反向定位未命中缓存时可能较慢。
