# 帧析 · 项目导入上下文

> 用途：将本文件作为后续 AI 任务、交接或新会话的项目上下文。它汇总截至 2026-09-16 的项目目标、行为约束、架构、运行方式与当前工作区状态。详细原文以 `README.md`、`首版需求方案.md`、`项目架构与代码学习指南.md` 和 `AGENTS.md` 为准。

## 1. 项目概览

- 名称：帧析 · 视频处理工具 Demo，当前版本 `v0.2.0`。
- 平台与技术：Windows 本地 Python + PyQt5 客户端；OpenCV 负责解码，FFmpeg 负责剪辑与转码，SQLite 保存本地任务状态。
- 三个独立功能页：帧标记与图片导出、视频剪辑、视频格式转换。
- 目标是实用、简洁的本地初步处理工具；打包、安装和分发尚未纳入范围。

## 2. 用户可见行为与不可变约束

### 帧标记与导出

- 可导入最多 10 个视频；单个最长 3 分钟，最高 2560×1440（或对应竖屏）。支持 MP4、MKV、AVI、MOV、WebM、M4V，实际可读性依赖本地解码器。
- 帧号与原图像素坐标均从 0 开始。支持播放/暂停、逐帧、输入帧号、点击或拖动时间轴定位、缩放、平移与适应窗口。
- 浏览帧不会自动标记。标记仅表示“需要导出的帧”，不是审核结论、检测框或区域标注。
- 每段视频单独保存标记和查看位置；视频身份由内容 SHA-256 摘要确定，同内容的不同路径会关联同一记录。
- 仅导出当前视频中明确标记的帧，格式为 PNG 或 JPG；以原始解码帧、原分辨率导出，不含窗口控件、缩放、平移或亮度提示。
- 每次图片导出创建新的时间戳子目录，文件名含帧号，且不得覆盖已有图片。
- 缓存约上限 48 MiB；反向跳转若未命中缓存可从头顺序解码，因此可能较慢。

### 亮度审查

- 仅在暂停且已加载画面时允许开启；播放、切帧或切视频会关闭并清除提示，暂停后不能自动重新开启。
- 指针在画面同一位置停留 0.2 秒，显示原图像素坐标及亮度值 0–255。
- 亮度定义：`int(0.299 * R + 0.587 * G + 0.114 * B + 0.5)`；为 RGB 加权灰度，不是物理亮度。
- 缩放与平移后仍映射至原图像素；图片外的黑边不采样；移动、离开、拖动或缩放会清除旧提示。

### 剪辑与格式转换

- 剪辑为单一时间区间：可预览、播放/暂停、逐帧、时间轴定位，在当前帧设起点或“最后保留帧”，并预览选区。
- 时间区间始终为起点包含、终点不包含。若将第 `j` 帧设为最后保留帧，终点为 `(j + 1) / fps`。
- 剪辑与转换不受帧标记页的时长、分辨率限制。二者一次各处理一个视频，提供进度和取消。
- 输出格式为 MP4、MKV、AVI。MP4/MKV 使用 H.264 + AAC；AVI 使用 MPEG-4 + MP3；保留首条视频流与可选首条音轨。
- 一律重新编码；奇数宽高补齐至偶数；不承诺无损。先写临时文件，验证可读后再变为最终输出；不得覆盖源文件或已有输出。
- 预览不播放音频。时间通过帧号/FPS 换算，尚未支持可变帧率精确时间戳。

### 明确移除或暂不支持

- 不重新引入审核状态、问题分类/备注、批量审核、检测数据导入、检测叠加、左右对照、统计或 Excel/CSV 导出。
- 不支持视频拼接、多区间删除、批量剪辑、批量转换、自定义编码参数、字幕和多音轨保留。
- 不支持独立图片处理、打包或分发。
- 旧 `.demo-data/review.sqlite3` 必须保留但不得读取或删除。当前标记数据库为 `.demo-data/frames.sqlite3`，同样不得清理。

## 3. 架构与模块边界

| 路径 | 职责 |
| --- | --- |
| `main.py` | 应用入口，创建 `QApplication` 与 `MainWindow` |
| `media_tool/ui.py` | 主窗口、后台任务协调、剪辑/转换页 `ProcessPage` |
| `media_tool/frame_page.py` | 帧导入、定位、标记、恢复与导出交互 |
| `media_tool/clip_preview.py` | 剪辑画面预览、区间选择和区间播放 |
| `media_tool/widgets.py` | `ImageView`、亮度悬停提示、`TimelineSlider` |
| `media_tool/media.py` | `VideoInfo`、`FrameReader`、视频检查、FFmpeg 转码 |
| `media_tool/rendering.py` | OpenCV BGR 帧转换为 Qt RGB 图片 |
| `media_tool/frame_store.py` | SQLite 标记和查看位置持久化 |
| `media_tool/exports.py` | 用独立读取器导出已标记原始帧 |
| `media_tool/workers.py` | 后台 `Job` 与信号 |
| `media_tool/demo.py` | 本地演示视频生成 |
| `tests/test_demo.py` | 基础媒体、持久化、导出、转码测试 |
| `tests/test_revision.py` | 时间轴、最后定位请求、界面稳定性、剪辑回归测试 |

架构规则：业务与界面分离；公共媒体能力复用既有接口；禁止循环依赖和跨模块修改内部状态；不为拆分新增无意义转发层或预先引入插件系统。耗时读取、导出、剪辑和转码必须在后台运行，并通过 Qt 信号在主线程更新界面。

调用要点：`MainWindow` 创建 `FrameStore`、`FramePage` 和两个 `ProcessPage`；`FramePage` 导入后后台检查视频和计算内容摘要，再读取帧；`FrameReader` 按需解码并缓存；`exports.py` 单独读取帧避免改变播放器位置；`transcode()` 使用参数列表启动 FFmpeg。

## 4. 并发、状态与数据细节

- `MainWindow.run_job()` 使用 `Job` 处理后台操作；GUI 对象只能在主线程更新。全局 `busy` 使此类任务串行。
- 解码忙碌时只保留最后一次定位请求；结果带视频身份和帧号，若视频已切换或请求过期则丢弃，禁止旧结果回退画面。
- `TimelineSlider.seekRequested` 表示用户定位；`set_frame_value()` 是播放更新且会阻断信号。点击或拖动时间轴必须暂停播放。
- 更新帧画面不应反复重建标记列表或禁用/启用右栏；仅在视频切换或标记数据改变时更新列表，并保留选择状态。
- `FrameStore` 有 `videos(id, path, position)` 与 `marks(video, frame)` 两张业务表；`marks` 的联合主键 `(video, frame)` 保证去重。数据库连接在主线程使用，不得直接传给后台任务。
- 显示新帧时用 `loading` 区分程序设置复选框与用户点击，避免错误写入标记。
- `FrameReader` 的反向未命中策略是重新打开并从头顺序解码；优化前须保留结果准确性的测试。

## 5. 环境、启动与验证

依赖：

```text
PyQt5>=5.9,<6
opencv-python>=4.5,<5
numpy>=1.19,<3
imageio-ffmpeg>=0.4.9,<0.7
```

PowerShell 常用命令：

```powershell
python -m venv .venv
.venv\Scripts\python -m pip install -r requirements.txt
.venv\Scripts\python main.py
.venv\Scripts\python -m unittest discover -s tests -v
```

也可使用已可用解释器执行 `python main.py` 与 `python -m unittest discover -s tests -v`，或双击 `启动Demo.cmd`。FFmpeg 查找顺序是 `FFMPEG_EXE`、PATH、`imageio-ffmpeg`、`.local/ffmpeg-path.txt`；不可将个人绝对路径写入代码。

功能变更须补充实际行为测试；媒体、持久化、跨模块行为变更须运行完整 unittest 集。UI 改动还应在图形环境检查启动与受影响交互，生成截图放在 `test-output/`。测试应使用临时目录与样例视频，绝不修改用户真实视频或已有数据库。

## 6. 仓库协作与数据保护

- 修改前阅读 `README.md`；涉及行为查阅 `首版需求方案.md`；涉及架构查阅 `项目架构与代码学习指南.md`。先检查相关代码、测试和工作区差异，保留已有变更，避免无关重构或批量格式化。
- 使用 `pathlib.Path`；外部进程使用参数列表，避免拼接含用户路径的 shell 命令。新增依赖必须有明确需求、记录到 `requirements.txt`，并说明兼容性和维护成本。
- `.demo-data/`、`.local/`、`test-output/`、虚拟环境、缓存和日志均为本地数据或生成物，不纳入版本控制；不可提交凭据或个人机器配置。
- 产品行为、依赖、启动或验证方式变化时，同步更新 README 和相应需求/架构文档。
- 默认使用简体中文沟通和 UI 文案；代码标识符遵循模块既有风格。

## 7. 当前工作区状态（2026-09-16 快照）

以下更改已存在，归属用户或当前未提交工作，后续操作必须保留，除非明确授权：

```text
已修改：README.md、main.py、media_tool/__init__.py、media_tool/frame_page.py、
        media_tool/ui.py、media_tool/widgets.py、项目架构与代码学习指南.md、首版需求方案.md
未跟踪：AGENTS.md、CHANGELOG.md、media_tool/clip_preview.py、tests/test_revision.py
```

`CHANGELOG.md` 记录 v0.2.0：新增可视化剪辑；修复时间轴定位、解码期间保留最后请求、切换视频丢弃旧画面、右栏闪烁和同尺寸画面重复适配。其记录称完整 unittest 当时为 15 项通过；任何后续任务应自行重新验证，不能将该历史记录视为本次验证结果。

## 8. 已知限制与后续改进边界

- 可变帧率没有精确时间戳支持。
- 反向随机定位在缓存未命中时可能慢。
- 后台任务串行，每项任务新建线程；未来如优化持续高帧率播放，需保持媒体句柄串行访问和帧准确性。
- `MainWindow` 集中协调任务，页面通过主窗口使用服务；这是 Demo 的务实取舍，只有实际需求出现时再提取接口或调度模块。
- 图片导出是逐张写入，失败时先前成功图片可能保留，并非全有或全无事务。
- 依赖有版本范围但未提供完整锁定文件，新环境安装后需重新验证。

## 9. 推荐阅读与排障顺序

1. `main.py` → `MainWindow.__init__()` → `FramePage.import_files()` → `request_frame()` → `show_frame()`。
2. `FrameReader` → `to_image()` → `FrameStore` → `export_images()`。
3. `Job` → `MainWindow.run_job()` → `ImageView` 亮度方法 → `transcode()` → 测试。

阅读函数时回答：它接收什么、改变或返回什么、出错时由谁处理。排查行为问题先检查需求约束和对应回归测试，再以最小改动修复。
