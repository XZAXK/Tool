# 仓库协作规则

本文件记录本仓库长期适用、可执行、可验证的开发约定。适用于整个仓库；子目录如有更具体的 `AGENTS.md`，在其范围内遵循对应规则。用户当前明确要求优先于本文件中的默认约定。

## 项目背景与阅读入口

- 本项目是面向 Windows 的本地 Python + PyQt5 视频处理客户端，包含帧标记与导出、视频剪辑、视频格式转换三个页面。
- 开始修改前阅读 `README.md`；涉及产品行为时查阅 `首版需求方案.md`，涉及架构时查阅 `项目架构与代码学习指南.md`。
- 先检查相关代码、测试和工作区差异，保留用户已有修改；避免与任务无关的重构或批量格式化。
- 默认用简体中文沟通、编写用户界面文案和项目说明；代码标识符遵循所在模块的风格。

## 环境与常用命令

在仓库根目录执行以下命令；使用已经安装项目依赖的 Python 解释器。

```powershell
# 新环境初始化（已有可用环境时无需重复）
python -m venv .venv
.venv\Scripts\python -m pip install -r requirements.txt

# 启动客户端
.venv\Scripts\python main.py

# 执行测试
.venv\Scripts\python -m unittest discover -s tests -v
```

- 已激活虚拟环境或使用其他可用环境时，可直接使用 `python main.py` 和 `python -m unittest discover -s tests -v`。
- Windows 也可通过 `启动Demo.cmd` 启动；修改启动方式时同步检查此脚本和 README。
- FFmpeg 查找顺序为 `FFMPEG_EXE`、PATH、`imageio-ffmpeg`、`.local/ffmpeg-path.txt`；复用既有查找逻辑，不把个人机器的绝对路径写入代码。
- 依赖统一记录在 `requirements.txt`。优先复用标准库、Qt 和现有依赖；新增依赖须有具体需求，并说明兼容性与维护成本。

## 模块边界与实现约定

| 模块 | 职责 |
| --- | --- |
| `main.py` | 应用启动 |
| `media_tool/ui.py` | 主窗口、剪辑与转换页面 |
| `media_tool/frame_page.py` | 帧定位、标记与导出交互 |
| `media_tool/widgets.py` | 共用画面控件与鼠标交互 |
| `media_tool/media.py` | 媒体信息、解码与 FFmpeg 处理 |
| `media_tool/frame_store.py` | 标记与查看位置的 SQLite 持久化 |
| `media_tool/exports.py` | 标记帧图片导出 |
| `media_tool/rendering.py` | 视频帧转 Qt 图片 |
| `media_tool/workers.py` | 后台任务与信号 |
| `media_tool/demo.py` | 演示视频生成 |

- 业务与界面分离，公共媒体能力通过已有接口复用；禁止循环依赖和跨模块修改内部状态。
- 不为拆分增加无意义的转发层，不预先引入插件系统。
- 耗时读取、导出与转码放在后台执行；通过 Qt 信号向界面报告结果、进度和错误，界面控件在主线程更新。
- 缓存必须有上限；处理完成、失败或取消后释放媒体句柄、线程和临时资源。
- 文件路径优先使用 `pathlib.Path`；调用外部进程时使用参数列表，避免拼接含用户路径的 shell 命令。

## 必须保持的产品与数据约束

以下是当前行为约定；用户明确要求变更时，同步修改实现、相关测试和需求文档。

- 帧号与原图像素坐标从 0 开始；剪辑时间区间为起点包含、终点不包含。
- 浏览帧不自动标记；图片导出仅包含当前视频明确标记的帧，保持原分辨率和原画面内容。
- 缩放、平移与亮度审查仅影响查看交互，不改变原图或导出图片；亮度从原始解码画面的对应像素计算，黑边不采样。
- 帧标记页面的数量、时长和分辨率限制不能直接套用到剪辑与转换页面；具体数值以需求文档为准。
- 不覆盖源视频和已有输出。图片导出创建新子目录；转码先写临时文件，验证成功后再转为目标文件。
- 保留 `.demo-data/frames.sqlite3` 中的标记和查看位置。旧版 `review.sqlite3` 不再读取，也不得为清理或测试而删除。
- 未经明确需求，不重新引入已移除的审核分类、检测数据导入或 Excel/CSV 分析记录导出。
- `.demo-data/`、`.local/`、`test-output/`、虚拟环境、缓存和日志是本地数据或生成物，不纳入版本控制；不提交凭据或个人机器配置。

## 验证与交付

- 行为变更优先验证受影响功能；涉及媒体处理、持久化或跨模块交互时运行完整的 unittest 测试集。
- 修复缺陷或新增逻辑时，在现有 `tests/` 中补充能验证实际行为的测试；纯文档修改无需运行媒体处理测试。
- 测试使用临时目录和生成的样例视频，不修改用户真实视频或已有任务数据库。
- 界面修改除相关自动化测试外，还应在可用图形环境中检查启动与受影响交互；截图等生成物放入 `test-output/`。
- 无法运行测试或图形验证时，说明缺失条件和未验证范围，不把未执行的检查表述为通过。
- 产品行为、依赖、启动或验证方式变化时，同步更新 README 和相关文档。
- 完成后简要说明修改内容、验证结果及尚未解决的问题。

## 本文件的维护原则

- 适合写入：长期协作偏好、环境命令、模块边界、编码约定、业务不变量、数据保护要求、验证标准和交付要求。
- 每条规则尽量写清触发条件、具体动作和可检查结果，避免“提高质量”等无法执行的泛泛要求。
- 临时任务、进度记录、完整需求、长篇教程和频繁变化的机器路径放在其他文档中；此处保留必要链接。
- 规则随项目演进更新，删除过时或冲突内容；子目录专属规则放在对应目录，避免重复维护。
