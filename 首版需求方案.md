# 视频与图像处理工具：首版需求方案

当前版本按最新需求移除帧审核模块，替换为**帧标记与导出**。本文件描述当前范围，取代此前关于审核和分析记录导出的约定。

当前实现版本：**v0.2.0**，增加可视化剪辑并修复时间轴定位和右栏闪烁。

## 1. 项目定位

本地视频与图像初步处理工具，采用 Python + Qt，面向 Windows。界面简洁易用，以实用性为优先。打包、安装与分发后续考虑。

| 模块 | 用途 |
| --- | --- |
| 帧标记与导出 | 导入视频，定位并标记需要保存为图片的帧 |
| 视频剪辑 | 截取指定时间区间，生成新视频 |
| 视频格式转换 | 转换视频格式，生成新视频 |

## 2. 架构原则

- 各模块独立、高内聚、低耦合，通过明确接口协作，禁止循环依赖及跨模块修改内部状态。
- 业务与界面分离，公共媒体读取及处理能力统一复用。
- 不为拆分而增加大量转发层，不预先引入插件系统。
- 实现前先查询已有模块接口、本地库、标准库和 Qt 能力，再评估更简单实现；有实际缺口时才新增依赖或自行实现。
- 新依赖考虑适配、兼容和维护成本。
- 耗时读取、导出和转换后台执行，缓存有上限。

## 3. 帧标记与导出

### 输入

- 单个及批量导入，当前列表最多十个视频。
- 单个不超过三分钟；2K 暂按最高 2560×1440 或对应竖屏实现。
- 仅需视频，不导入检测标记文件，不从画面反推检测数据。
- 可输入已绘制检测结果的视频，画面内容原样保留。
- 选择器提供 MP4、MKV、AVI、MOV、WebM、M4V，实际读取能力取决于本地解码器。

### 查看和标记

- 单画面展示，支持播放/暂停、上一帧/下一帧、指定帧跳转。
- 点击或拖动时间轴暂停播放并定位；读取繁忙时保留最后一次定位请求，不让旧读取结果回退画面。
- 逐帧切换不反复禁用右侧功能栏或重建标记列表，保留列表选择状态。
- 支持缩放、平移和适应窗口。
- 帧标记指选中需要导出的帧，不代表审核结论，也不是框或区域标注。
- 可标记、取消标记当前帧，已标记帧列表支持定位和取消。
- 各视频独立保存标记与查看位置，支持恢复任务。
- 浏览帧不会自动标记。
- 提供“亮度审查”按钮，仅暂停且画面加载完成时可开启；播放期间禁用，开始播放自动关闭。
- 开启后指针悬停同一位置 0.2 秒显示对应原图像素亮度（0–255，RGB 加权灰度）及坐标。缩放、平移后仍对应原图像素，不对黑边采样；移动、离开、切帧时清除旧提示。

### 导出

- 只导出当前视频中明确标记的帧。
- PNG、JPG，保持原分辨率和画面内容，不额外叠加检测信息。
- 创建新的子目录，文件名包含帧编号，避免覆盖。
- 不再提供 Excel/CSV 分析记录导出。

## 4. 视频剪辑

- 独立选择视频与输出文件。
- 截取单时间区间，支持画面预览、播放/暂停、逐帧定位及点击或拖动时间轴。
- 可将当前帧设为起点或最后保留帧；绿色时间轴显示选区，支持预览选区及选择全部。
- 起止时间与画面选区同步，也可按秒输入。起点包含、终点不包含；设置最后保留帧时，终点取该帧之后的边界。
- 预览不播放音频；时间按帧号与 FPS 换算，可变帧率精确时间戳尚未实现。
- 输出 MP4、MKV、AVI，保留首条音轨（如有）。
- 重新编码至新文件，提供进度与取消。
- 不套用帧标记模块的时长和尺寸限制。
- 拼接、多区间删除、批量剪辑尚未纳入当前实现。

## 5. 视频格式转换

- 独立选择视频和 MP4、MKV、AVI 输出。
- 每次处理一个视频，保留首条视频流与可选首条音轨。
- MP4/MKV 使用 H.264 + AAC，AVI 使用 MPEG-4 + MP3。
- 提供进度、取消，拒绝覆盖源视频和已有文件。
- 批量转换、自定义编码参数、字幕和多音轨处理尚未纳入实现。

## 6. 已移除或范围外

- 审核状态、误检/漏检/分割问题分类、问题备注和批量审核。
- 检测统计、审核进度和分析表格导出。
- 检测标记文件导入、检测叠加绘制和左右对照。
- 检测框编辑、分割编辑、目标追踪和多模型对比。
- 独立图片处理、打包和分发。

旧审核数据库保留在本地，新版使用独立帧标记数据库；旧审核记录不自动转换为新标记。

## 7. 验收

1. 无需标记文件即可导入视频，同目录 JSON 不影响导入。
2. 逐帧与跳转画面对应实际解码帧。
3. 标记去重，取消有效，各视频记录隔离，关闭后可恢复。
4. PNG/JPG 只含明确标记的帧，尺寸与输入一致。
5. 不再显示审核、问题分类、检测统计和表格导出入口。
6. 剪辑与转换独立可用，输出可读取，源视频不被覆盖。
7. 耗时操作不阻塞界面，失败有明确提示。
8. 时间轴点击及连续拖动定位到最后选择位置并暂停播放，视频切换后不显示旧视频的解码结果。
9. 每帧切换不触发右栏反复禁用、启用或标记列表重建。
10. 可在画面上确定剪辑起止帧并预览选区；固定帧率样例的实际输出帧数和时长符合所选范围。

## 8. 实现入口与限制

Demo 帧号从 0 开始，时间使用帧号除以 FPS，可变帧率精确时间戳暂未实现。浏览不含音频播放，反向跳转未命中缓存时需重新顺序解码。

根目录 `main.py` 或 `启动Demo.cmd` 为入口。使用说明、依赖及测试命令见 [README.md](README.md)。
