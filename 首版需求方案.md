# 视频与图像处理工具：需求方案

更新日期：2026-09-15  
文档状态：需求讨论汇总；待确认项不视为已确定的实现约定。

## 1. 项目定位

项目面向视频和图像的初步处理，采用可扩展的本地客户端架构。目前明确需要三个功能模块：

| 功能模块 | 用途 | 需求状态 |
| --- | --- | --- |
| 检测结果逐帧审核 | 检查算法误检、漏检及分割不准确问题 | 已完成主要功能讨论，数据格式待样例确认 |
| 视频剪辑 | 对视频进行剪辑并生成处理结果 | 已确认需要，具体操作范围待讨论 |
| 视频格式转换 | 将视频转换为所需格式 | 已确认需要，输入输出格式及参数待讨论 |

逐帧审核只是工具中的一个功能，不代表整个项目的功能范围。此前关于审核模块不导出视频片段的约定，不限制独立的视频剪辑功能。

本文第 3～8 节描述审核模块；第 14、15 节记录新增功能。新增功能的详细验收及版本安排随需求讨论补充。

## 2. 基本约束

| 项目 | 首版要求 |
| --- | --- |
| 开发语言 | Python（替代此前讨论的 C++） |
| 客户端 | 本地 Qt 图形界面 |
| 操作系统 | 仅 Windows |
| 审核模块导入数量 | 一次最多 10 个视频 |
| 审核模块单个视频时长 | 不超过 3 分钟 |
| 审核模块分辨率 | 最高 2K，具体像素尺寸待确认 |
| 界面原则 | 简洁易用，以实用性为第一需求 |
| 打包与安装 | 首版完成后再考虑，首版先在开发环境运行 |

上述数据规模来自审核流程讨论，是否同样适用于剪辑和格式转换，需另行确认。

## 3. 审核模块：输入数据

- 支持单个视频导入和多个视频批量导入。
- 每个视频对应一个包含整段视频检测结果的标记文件。
- 不支持每帧一个标记文件的组织方式。
- 每个视频只关联一份检测结果，不进行多模型或多参数结果对比。
- 支持矩形检测框和分割结果，两者可在同一视频中同时存在。
- 使用原始视频与标记文件生成检测叠加画面；已绘制检测结果的视频不作为必需输入。
- 显示文件中的全部检测结果，不提供类别或置信度筛选。
- 类别、置信度等信息依据文件实际内容展示，不补造缺失数据。

标记文件的具体格式、字段和视频帧对应方式，须通过实际样例确认。扩展名本身不构成解析约定。

## 4. 查看与对照

### 4.1 对照布局

- 左侧显示原始视频帧。
- 右侧显示同一帧叠加检测框、分割结果及相关文字。
- 两侧播放、暂停、逐帧切换和跳转保持同步。
- 两侧缩放和平移联动，方便核验同一区域。

### 4.2 基本操作

- 播放与暂停。
- 上一帧、下一帧。
- 跳转指定帧。
- 选择需要导出的帧。
- 在视频列表中切换审核对象。

### 4.3 显示约定

使用固定显示样式，不提供检测框、分割轮廓、填充的独立开关，也不提供透明度调节。具体配色和填充样式在界面实现时确定。

## 5. 审核与问题记录

审核以帧为单位，不要求指出具体错误框或圈出错误区域。

| 内容 | 规则 |
| --- | --- |
| 审核状态 | 未审核、已审核 |
| 问题类型 | 误检、漏检、分割不准确 |
| 多问题记录 | 同一帧可同时具有多种问题 |
| 备注 | 支持文字备注 |
| 单帧确认 | 支持手动确认审核完成 |
| 批量确认 | 支持将选定的连续帧标记为已审核 |

- 浏览过某帧不自动代表已审核。
- 审核状态与问题记录分别维护。
- 批量确认只更新审核状态，不覆盖问题类型和备注。
- 已审核且无问题标记，表示人工确认该帧无上述问题。

## 6. 本地保存与恢复

保存以下内容，以便关闭后继续审核：

- 视频与标记文件的关联。
- 各帧审核状态。
- 误检、漏检、分割不准确标记及备注。
- 每个视频上次查看的位置。

本地保存与文件导出是两项独立能力。具体存储格式、保存时机以及源文件移动后的处理方式，在实现设计阶段确定。

## 7. 统计

### 7.1 统计内容

- 每帧检测数量。
- 各类别累计检测次数。
- 各视频总帧数、已审核帧数及审核进度。
- 误检帧数、漏检帧数、分割不准确帧数。
- 去重后的问题帧总数。
- 视频列表的汇总信息。

### 7.2 统计口径

- 检测次数按各帧的检测记录累计，同一目标跨帧出现会重复计入。
- 不进行目标追踪，不统计独立目标数量或独立目标出现次数。
- 同一帧存在多类问题时，分别计入对应分类；问题帧总数按帧去重。
- 同一条检测记录中的检测框和分割结果只计一次；具体关联规则待样例确认，不直接将框数与分割区域数相加。
- 首版不计算准确率、召回率等需要完整真实标注及匹配规则的指标。

## 8. 导出

### 8.1 图片导出

- 只导出用户明确选中的帧，不自动导出全部问题帧。
- 导出带检测框、分割结果及相关文字的图片，对应右侧检测结果画面。
- 同时支持 PNG 和 JPG。
- 首版不要求导出原始无标记图片或左右对照合成图。
- 导出选择与审核状态、问题标记相互独立。

### 8.2 分析记录导出

- 同时支持 Excel（`.xlsx`）和 CSV。
- 仅导出标记了问题的帧，不导出所有已审核帧。
- 按视频分别生成文件，不将多个视频合并成一张表。
- 每个问题帧一行，同一帧的多种问题记录在同一行。

导出字段：

| 字段 | 内容 |
| --- | --- |
| 视频名称 | 对应的视频文件名称 |
| 帧编号 | 问题所在帧，编号基准待统一 |
| 时间位置 | 该帧在视频中的时间位置 |
| 误检标记 | 是否存在误检 |
| 漏检标记 | 是否存在漏检 |
| 分割不准确标记 | 是否存在分割不准确 |
| 备注 | 人工填写的说明 |

无问题帧视频的导出行为、文件命名和同名文件处理，在实现设计阶段确定。

## 9. 架构原则

### 9.1 模块独立

- 按业务职责组织模块，保持高内聚、低耦合，方便增添功能及修改需求。
- 模块通过明确且尽量简单的接口协作。
- 禁止循环依赖和直接修改其他模块内部状态。
- 将复杂处理封装在模块内部，避免仅为拆分而增加大量转发层。
- 业务逻辑与 Qt 界面分离，核心处理可以脱离界面验证。
- 首版采用普通模块化结构，不预先引入缺少实际需求的插件系统。

### 9.2 建议职责划分

以下为设计起点，具体目录和接口需结合项目及本地依赖检查后确定。

| 模块 | 职责 |
| --- | --- |
| 任务管理 | 管理视频列表、输入关联和任务切换 |
| 视频读取 | 获取视频信息、准确定位帧、维护有限缓存 |
| 检测数据读取 | 解析标记格式并转换为统一检测数据 |
| 检测绘制 | 提供界面与图片导出共用的检测绘制能力 |
| 审核记录 | 管理审核状态、问题、备注和本地恢复 |
| 统计 | 根据检测数据及审核记录计算统计结果 |
| 导出 | 生成选中帧图片与 Excel、CSV 问题记录 |
| 视频剪辑 | 管理剪辑操作并调用公共媒体处理能力输出视频 |
| 视频格式转换 | 管理转换参数并调用公共媒体处理能力输出视频 |
| 公共媒体处理 | 封装可复用的视频信息读取及实际需要的媒体处理能力 |
| Qt 界面 | 展示数据、接收操作、反馈状态 |

审核、剪辑和格式转换应能够独立使用。剪辑和格式转换不以检测标记文件为必需输入，也不依赖审核状态。共用能力通过接口复用，避免在各功能中重复实现媒体处理逻辑。

### 9.3 代码生成前的复用检查

每项功能实现前，按以下顺序检查：

1. 查找项目现有实现和其他模块的可复用接口。
2. 检查本地已有 Python 库、标准库及 Qt 自带能力。
3. 判断是否可通过更简单的设计满足需求。
4. 确实存在能力缺口时，再评估新增依赖或自行实现。

复用需考虑功能适配、版本兼容、许可证和维护成本。不能仅为减少代码行数引入不合适的依赖或不合理的跨模块调用。

## 10. 性能与异常处理

- 主要按需读取、缓存当前视频，避免同时加载全部视频帧。
- 缓存需有上限，不能将整段视频所有解码帧无约束地放入内存。
- 视频读取、数据解析及批量导出等耗时任务避免阻塞界面。
- 输入无法读取、关联存在歧义或检测数据无法对齐时，给出具体原因。
- 无法确认帧对应关系时，不使用猜测结果进行审核。
- 加载、执行成功和失败需有明确反馈。
- 首版具体响应时间及内存指标，结合代表性样例与运行电脑测定。

## 11. 当前不包含或尚未定义的能力

- 审核模块内的视频片段导出；独立视频剪辑功能已纳入项目需求，见第 14 节。
- 独立图片导入与处理流程。
- 检测框新增、删除、调整及类别修改。
- 分割区域编辑和错误区域标注。
- 关键点支持。
- 目标追踪、独立目标计数。
- 多模型或多参数结果对比。
- 类别与置信度筛选。
- 检测显示开关及透明度调节。
- 每帧一个标记文件的导入方式。
- 打包、安装与分发。

## 12. 待确认事项

| 事项 | 确认方式 |
| --- | --- |
| 标记文件格式、字段及支持的格式范围 | 检查实际文件样例 |
| 帧编号或时间戳关联方式 | 检查样例及生成规则 |
| 帧编号起点与时间精度 | 结合输入约定统一内部及显示规则 |
| 分割结果为多边形、掩码或其他表示 | 检查实际文件样例 |
| 检测框与分割结果的关联关系 | 检查数据结构 |
| 无检测记录表示无目标还是数据缺失 | 确认检测工具输出约定 |
| 类别名称映射来源 | 检查标记文件及配套文件 |
| 2K 实际尺寸、编码和帧率 | 检查代表性视频 |
| Qt 绑定及视频、表格处理依赖 | 检查本地环境后选择 |
| 本地保存方式及源文件移动处理 | 实现设计阶段确定 |
| 导出命名、重名处理及空记录行为 | 实现设计阶段确定 |

## 13. 审核模块首版验收重点

1. 支持约定规模内的单个及批量导入，对无效输入提供明确提示。
2. 左右画面始终对应同一帧，检测结果准确叠加到对应画面。
3. 同时显示检测框与分割结果，并正确展示文件提供的相关信息。
4. 单帧问题记录、连续帧批量审核与统计结果一致，批量审核不覆盖问题记录。
5. 重新打开后能够恢复审核记录与查看位置。
6. 仅选中帧生成带检测结果的 PNG 或 JPG 图片。
7. Excel 与 CSV 按视频分别导出，仅包含问题帧，与本地记录一致。
8. 数量统计遵循记录关联与去重规则，不将检测次数解释为独立目标数量。
9. 逐帧操作和耗时任务期间界面能够响应，缓存与内存使用受到控制。

开发前需先用实际视频与标记文件确认数据约定，再选定依赖和实现模块接口。

## 14. 视频剪辑模块

### 14.1 已确认需求

- 工具需要提供视频剪辑功能。
- 剪辑作为独立功能模块组织，复用项目公共能力。

### 14.2 待讨论范围

以下为待确认事项，不代表全部需要实现：

- 剪辑操作：截取一个时间段、删除中间片段、多片段拼接等需要哪些。
- 起止位置选择方式，以及是否要求精确到帧。
- 音频保留、移除或其他处理需求。
- 输出格式、画质要求及文件命名规则。
- 是否需要批量操作。
- 是否适用审核模块的数量、时长及分辨率限制。
- 是否需要同步处理检测标记文件；目前不自动纳入范围。

具体操作流程、版本安排及验收标准，在上述范围明确后补充。

## 15. 视频格式转换模块

### 15.1 已确认需求

- 工具需要提供视频格式转换功能。
- 格式转换作为独立功能模块组织，复用项目公共能力。

### 15.2 待讨论范围

以下为待确认事项，不代表全部需要实现：

- 需要支持的输入与输出格式。
- 是否仅改变封装格式，或还需要转换视频、音频编码。
- 是否需要调整分辨率、帧率、码率或画质。
- 是否需要批量转换及任务进度、取消操作。
- 音频保留规则、输出位置及重名处理。
- 是否适用审核模块的数量、时长及分辨率限制。

实现前先检查本地可用库、媒体处理工具及现有模块接口，再选定实现方式。具体操作流程、版本安排及验收标准，在范围明确后补充。
